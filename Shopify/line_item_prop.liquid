var formContents = available_ids[i].split('-');
                    var qty = formContents[1];
                    var id  = formContents[0];//variant_id
                    if(formContents[2] != null){
                      var properties= {"_source":"Rebuy","_attribution":"Rebuy Tiered Progress Bar","_tier": formContents[2] };
                    }
                    var data = {quantity:qty,id:id};
                        if(properties != null){
                            data.properties = properties;
                        }
                     var params = {
                      type: 'POST',
                      url: '/cart/add.js',
                      data: data,
                      dataType: 'json',
                      success: function(line_item) {
                        var iteration_length = i+1;
                        //if iteration length is equal to the total length - it means that we have
                        //   $(this).removeClass('bundle-cart-active');
                        //   // document.location.href = '/checkout';
                        //   // document.location.href = '/checkout?discount=test123';
                        // }
                      },
                      error: function() {
                          // console.log("fail");
                    $(this).removeClass('bundle-cart-active');
                      }
                  };